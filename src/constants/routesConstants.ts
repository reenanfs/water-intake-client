export enum clientRoutePaths {
	HOME = '/',
	SETTINGS = '/settings',
	LOGIN = '/login',
	REGISTER = '/register',
}

export enum serverRoutePaths {
	LOGIN = '/auth/login',
	REGISTER = '/auth/register',
	LOGOUT = 'auth/logout',
	PROFILE = '/auth/profile',
}
